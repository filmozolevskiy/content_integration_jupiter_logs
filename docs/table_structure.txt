# Jupiter API Tags Table

CREATE TABLE jupiter.jupiter_api_tags
(
    `username` String,
    `trip_id` UInt32,
    `booking_id` UInt32,
    `labels` Array(String),
    `timestamp` DateTime
)
ENGINE = Distributed('ota', 'jupiter', 'jupiter_api_tags_shard', rand())
